machine:
  node:
    version: 6.10.2

dependencies:

  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install
    - npm install --dev

checkout:
  post:
    - git submodule update --init

test:
  override:
    - npm test:unit